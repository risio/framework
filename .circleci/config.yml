# https://github.com/circleci/frontend/blob/master/.circleci/config.yml

version: 2
jobs:
  build:
    docker:
      - image: node:8.11

    working_directory: ~/app

    steps:
      - checkout

      - run: yarn
      - run: yarn build