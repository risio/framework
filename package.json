{
  "private": true,
  "version": "0.0.36",
  "repository": {
    "type": "git",
    "url": "https://github.com/risio/framework.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "oao run-script --parallel --no-parallel-logs build",
    "build-watch": "oao run-script --parallel --no-parallel-logs build-watch",
    "test": "oao run-script --parallel --no-parallel-logs test",
    "test-watch": "oao run-script --parallel --no-parallel-logs test-watch",
    "playground": "cd playground && yarn start",
    "start": "playground"
  },
  "devDependencies": {
    "oao": "^1.5.1"
  },
  "dependencies": {
    "gh-pages": "^1.1.0"
  }
}
